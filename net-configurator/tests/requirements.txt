pytest>=9.0.3,<10          # >=9.0.3 clears CVE-2025-71176 (was --ignore-vuln'd)
pytest-cov>=7.0.0,<8       # pytest-cov <7 does not support pytest 9
pyyaml>=6.0,<7
ruamel.yaml>=0.18.0,<0.19
openpyxl>=3.1.0,<4
pytest-xdist>=3.6.0,<4     # >=3.6 for pytest 9 compatibility
httpx>=0.27.0,<0.29
# Linters — `make lint` (and the CI lint stage) require these; installed here
# so CI actually lints instead of silently passing (see issue #4).
flake8>=7.0.0,<8
yamllint>=1.35.0,<2
